import 'package:flutter/material.dart';

class AuthPageBanner extends StatelessWidget {
  const AuthPageBanner({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
